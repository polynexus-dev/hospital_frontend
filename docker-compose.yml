services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://13.235.143.251:8001/api/v1
    ports:
      - "8002:8002"
    restart: always
